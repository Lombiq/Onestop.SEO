@model Onestop.Seo.Models.SeoPart

@{
    var title = !String.IsNullOrEmpty(Model.TitleOverride) ? Model.TitleOverride : Model.GeneratedTitle;
}

@Html.TextBox("TitleOverride", title, new { @class = "text large onestop-seo-rewrite-value", data_generated_default = Model.GeneratedTitle })