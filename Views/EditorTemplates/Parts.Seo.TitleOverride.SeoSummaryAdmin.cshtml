@model Onestop.Seo.Models.SeoPart

@{
    var title = !String.IsNullOrEmpty(Model.TitleOverride) ? Model.TitleOverride : Model.GeneratedTitle;
}

@Html.TextBox("TitleOverride", title, 
    new { @class = "text onestop-seo-rewrite-value", data_generated_default = Model.GeneratedTitle, data_max_length = "70" })