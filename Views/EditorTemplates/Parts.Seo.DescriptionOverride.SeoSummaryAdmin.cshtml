@model Onestop.Seo.Models.SeoPart

@{
    var description = !String.IsNullOrEmpty(Model.DescriptionOverride) ? Model.DescriptionOverride : Model.GeneratedDescription;
}

@Html.TextBox("DescriptionOverride", description,
    new { @class = "text onestop-seo-rewrite-value", data_generated_default = Model.GeneratedDescription, data_max_length = "150" })